env_defaults: &env_defaults
  working_directory: ~
  docker:
    - image: circleci/node:14.15.1

version: 2.1
jobs:
  prepare:
    <<: *env_defaults
    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1.0-dependencies-{{ checksum "pnpm.lock" }}
            # fallback to using the latest cache if no exact match is found
            - v1.0-dependencies-

      - node/install-packages:
          pkg-manager: pnpm

      - run: pnpm install

      - run: pnpm add jest-junit -W

      - save_cache:
          paths:
            - node_modules
          key: v1.0-dependencies-{{ checksum "pnpm.lock" }}

      - persist_to_workspace:
          root: .
          paths:
            - node_modules

  test:
    <<: *env_defaults
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          command: pnpm run test --ci
          name: Run Tests
  build:
    <<: *env_defaults
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          command: pnpm run build
          name: Build Typescript Package

orbs:
  node: circleci/node@4.1.0
workflows:
  test:
    jobs:
      - prepare
      - build:
          requires:
            - prepare
      - test:
          requires:
            - build
